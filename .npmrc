# Hoisting is off by default in pnpm, and left off deliberately: a package that
# compiles only because of a transitive dependency it never declared will break the
# moment that dependency moves, and it will break in CI rather than here.
strict-peer-dependencies=false
auto-install-peers=true
